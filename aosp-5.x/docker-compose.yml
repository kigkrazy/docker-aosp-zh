version: "2"

services:
  aosp:
    image: aosp:5.x
    command:  cd /usr/local/aosp && source build/envsetup.sh && ping 127.0.0.1
    container_name: aosp-5.x
    volumes:
        - ~/aosp/ccache:/tmp/ccache
        - /usr/local/aosp-5.x:/usr/local/aosp
        - ~/.gitconfig:/root/.gitconfig
